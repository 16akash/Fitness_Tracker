apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fitness-tracker-production
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/JeevanC31/Fitness_Tracker.git'
    targetRevision: feature # Update to 'master' when merged
    path: Fitness_Chart
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: fitness-tracker-production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
